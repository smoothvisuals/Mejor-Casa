// Edit this file to change store contact details — no other code changes needed.
window.STORE_CONFIG = {
  // WhatsApp number in international format, digits only (no +, spaces, or dashes)
  whatsappNumber: "523313255288",
  storeName: "Mejor Casa",
};

// Used only by admin.html to know which GitHub repo to save edits to.
window.ADMIN_CONFIG = {
  owner: "smoothvisuals",
  repo: "Mejor-Casa",
  branch: "main",
  // Change this to your own PIN — it's just a speed bump against casual snooping, not real
  // security (the real protection is that saving requires your own GitHub token, which only
  // you have). Anyone with the admin URL but no PIN and no token can't change anything.
  pin: "480217",
};
